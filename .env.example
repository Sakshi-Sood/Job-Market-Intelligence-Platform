# 1. Copy the example env and fill in your values
cp .env.example .env

# 2. Change DB_HOST=db in .env (for Docker networking)

# 3. Build and run everything
docker-compose up --build

# 4. Open http://localhost:8501 in your browser